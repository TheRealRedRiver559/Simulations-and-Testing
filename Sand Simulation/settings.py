class Settings:
    WIDTH = 800
    HEIGHT = 800
    SIZE = (WIDTH, HEIGHT)

    MAX_FPS = 100

    CELL_SIZE = 5 #cell size in pixels

    window = None
    columns = HEIGHT // CELL_SIZE
    rows = WIDTH // CELL_SIZE
